# 计划：协作功能收尾与同步 (2026-05-08)

## 目标
完成“分布式协作与分支合并”功能的最终集成、测试与知识库同步。

## 已完成任务
1. **测试验证**：运行后端 93 个测试和前端 62 个测试，确保无回归。
2. **文档同步 (Neat-Freak)**：
   - 更新 `CLAUDE.md` 以反映最新的联邦架构、合并引擎及权限规则。
   - 更新 `README.md`，添加协作与物理合并功能的描述。
3. **分支合并**：
   - 将 `feature/collaboration-integration` 分支合并回 `main`。
   - 解决 `PublicationAccessController` 和相关测试中的架构冲突。
4. **清理工作**：
   - 删除了已合并的特性分支。
   - 清理了冗余的 Git worktree。

## 架构要点记录
- **合并策略**：采用物理迁入（Physical Merge），通过 `merged_{targetPubId}_` 前缀重映射所有人物和家庭 ID，确保 ID 空间隔离。
- **递归加载**：前端视图支持最高 3 层的动态分支缝合，无需物理合并即可预览完整树。
- **权限边界**：合并操作由主谱 `OWNER` 触发，且必须具备分支的 `VIEWER` 权限。

## 验收结果
- [x] 后端测试 BUILD SUCCESS
- [x] 前端测试 15 passed, 62 passed
- [x] CLAUDE.md 架构描述一致性检查
- [x] 分支清理完成
